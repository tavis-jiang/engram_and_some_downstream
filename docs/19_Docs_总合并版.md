# X-gram / Engram / ComEmbed 文档总合并版

日期：2026-07-20  
目的：作为 `docs/` 目录的统一入口，概括全部文档的主题分工与当前推荐口径。  
说明：本文件是总导航，不替代各专题的正式汇报文档。

---

## 1. bg

当前 `docs/` 已按连续编号统一整理，覆盖三条主线：

- `Baseline / X-gram` 对照实验。
- `Engram` 纠错、成功复现与 faircompare 对比。
- `ComEmbed on X-GRAM` 的方法接入、失败复盘与稳定化结果。

## 2. Objective

本文件回答：

1. 现在每份文档分别负责什么。
2. 哪些文档是正式汇报口径。
3. 后续新增内容应接在哪个层面。

## 3. Experimental Setup

### 3.1 配置对比
| 层级 | 文档 |
|:--|:--|··

### 3.2 文档分层

| 层级 | 文档 |
|:--|:--|
| 总入口 | `01`, `19` |
| Engram 正式汇报 | `14`, `15` |
| Engram 教学与过程 | `04`, `05`, `07`, `12` |
| ComEmbed 方法与执行 | `06`, `08`, `09`, `10`, `13` |
| ComEmbed 失败、修复与结果 | `11`, `16`, `17`, `18` |

## 4. Results

当前正式对外口径推荐：

- Engram 三模型对比：`docs/15_Engram_faircompare_三模型汇报报告.md`
- Engram 成功原因：`docs/14_Engram实验成功汇报报告.md`
-[text](../logs) ComEmbed 方法线汇报：`docs/18_ComEmbed_XGRAM组会报告.md`

## 5. Conclusion

`docs/` 已完成连续编号与统一格式整理。后续若继续新增文档，应优先追加到现有专题，而不是再创建新的平行说明文件。
