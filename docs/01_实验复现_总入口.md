# 完整复现教程

日期：2026-07-20  
目的：给出当前仓库中 `Baseline / X-gram / Engram / ComEmbed` 的统一复现入口。  
说明：本文件只保留稳定复现链路，不再保留历史阶段性的绕路方案。

---

## 1. bg

当前仓库的实验主线有四条：

- `Baseline` 预训练与下游评测。
- `X-gram` 预训练与下游评测。
- `Engram` 复现、纠错、正式训练与 faircompare 评测。
- `ComEmbed on X-GRAM` 的方法接入与稳定性验证。

## 2. Objective

复现时应先明确三件事：

1. 训练 run 的目标是什么。
2. 对应 checkpoint 后要跑哪些评测。
3. 哪份文档是该实验线的最终口径。

## 3. Experimental Setup

### 3.1 推荐顺序

1. 先跑 `Baseline`。
2. 再跑 `X-gram`。
3. 再跑 `Engram faircompare`。
4. 最后再进入 `ComEmbed` 方法线。

### 3.2 关键配置

| 实验 | 配置 |
|:--|:--|
| Baseline | `configs/faircompare_baseline_360m.yaml` |
| X-gram | `configs/faircompare_xgram_360m.yaml` |
| Engram | `configs/faircompare_engram_360m.yaml` |

## 4. Results

当前正式对比口径优先看：

- `docs/15_Engram_faircompare_三模型汇报报告.md`
- `docs/19_docs总合并版_统一格式.md`

## 5. Conclusion

如果只是要复现实验，不要从旧的失败 run 或历史教程直接开始，优先走当前 faircompare 口径。
